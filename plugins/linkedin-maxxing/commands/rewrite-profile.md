---
description: Rewrite the full LinkedIn profile as one coherent positioning unit
---

Invoke the `rewrite-profile` skill. Paste current profile + target audience: $ARGUMENTS

If `audit-profile` has not been run, prompt the user to run it first.
