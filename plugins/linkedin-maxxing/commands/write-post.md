---
description: Draft a LinkedIn text post in the user's voice with inline humanizer drafting constraints
---

Invoke the `write-post` skill. User input / angle: $ARGUMENTS

If the user has not given a specific angle, route to the `find-ideas` skill first instead of guessing.
